name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 24

      - run: npm install --legacy-peer-deps

      - name: Typecheck (backend + frontend)
        run: npm run typecheck

      - name: Build frontend (esbuild ESM modules)
        run: npm run build:frontend

      - name: Deploy to Cloudflare Workers
        run: |
          if [ -z "${{ secrets.COP_DB_BASE }}" ]; then
            echo "::error::Please set COP_DB_BASE secret"
            exit 1
          fi
          printf 'COP_DB_BASE=%s\n' "${{ secrets.COP_DB_BASE }}" > .secrets.env
          npx wrangler deploy --secrets-file .secrets.env
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}